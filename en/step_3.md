## Add a paragraph

Underneath your `<h2>` heading code, add the paragraph `<p>` tags.

Between the `<p>` tags, add some introductory text.

Your code should look like this:

```html filename="index.html" line_numbers="true" line_number_start="33" line_highlights="37"
    <!-- The main content for the webpage goes between the main tags -->
    <main>
      <section>
        <h2>Facial expressions</h2>
        <p>Take a look at these facial expressions and try them in your own drawings.</p>
      </section>
      <!-- The first drawing and instructions go here --> 

```

> [!TIP]
>
> Use `<p>` tags for blocks of text such as introductions, descriptions, or instructions.

## Now run your code

Your page should look like this:
![A text-only page that contains the heading, subheading, and paragraph text.](images/text_only_page.png)

Click the **Run** button and check that the page now shows the heading, subheading, and introduction paragraph.
